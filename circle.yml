machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose
    - sudo docker-compose -v
    - sudo docker -v

test:
  override:
    - docker-compose run -d --no-deps web
    - cd app; mocha

# deployment:
#   hub:
#     branch: master
#     commands:
#       - $DEPLOY